# F1 implementation plan and inspection baseline

1. Inspect first: the target directory was empty; Next.js, React, Tailwind, lint, formatter, Docker, environment files and existing components were absent. No Git repository exists in the target or ancestors. Do not initialize Git or push.
2. Inventory backend contracts read-only. With no accessible Qafilah Laravel source/docs, integrate zero endpoints. Record each missing capability. No backend edits.
3. Establish pinned current stable tooling, strict TypeScript, feature-oriented folders, one lockfile, validated environment handling and secure standalone deployment.
4. Build inert contract-driven transport, safe errors, deterministic authentication interfaces, scoped Query cache lifecycle and tests. Backend remains authority.
5. Build a compact original design system, protected shell boundary, honest production login/unavailable surface, and development-only overview/table/detail/form/interaction review.
6. Execute frozen install, lint, typecheck, unit/component/architecture tests, production build, audit, browser accessibility/responsive review and Docker build/boot. Check production fixture exclusion and report real integration limits explicitly.
7. Deliver architecture, design, contract, verification and independent-review documentation with exactly one F2 recommendation. A local commit is conditional on an existing safe Git repository; none was present.
